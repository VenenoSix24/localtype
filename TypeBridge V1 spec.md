------

# 📘 项目名称（暂定）

**TypeBridge v1**

手机输入 → 局域网 → 电脑文本注入工具

技术栈：

- 📱 手机端：Flutter
- 💻 电脑端：Rust（Windows 优先）
- 🔐 通信：WSS（WebSocket Secure）
- 🌐 仅支持局域网（v1）

------

# 🎯 一、产品核心定位

一个专注于“文本输入”的跨设备桥接工具。

不做：

- 鼠标控制
- 屏幕共享
- 文件传输
- 远程控制

只做：

> 稳定、低延迟、可控的文字输入

------

# 🧭 二、整体架构

```
Flutter (Client)
        ↓  WSS
Rust Desktop Server
        ↓
Windows SendInput (Unicode 注入)
```

------

# 🖥 三、桌面端（Rust）

## 3.1 核心职责

- 启动 WSS 服务器
- 管理连接
- 处理输入注入
- 管理配对
- 系统托盘
- 心跳 & 重连支持

------

## 3.2 技术栈

- tokio
- tokio-tungstenite
- rustls（自签证书）
- tray-icon
- windows crate（SendInput）

------

## 3.3 网络设计

### 协议：WSS（必须）

```
wss://<ip>:8765
```

使用自签证书。

Flutter 端允许 bad_certificate_callback。

------

## 3.4 配对机制（v1 简化版）

### 流程：

1. 手机发现电脑
2. 点击连接
3. 电脑弹出 6 位验证码
4. 手机输入验证码
5. 验证成功后生成 session token
6. 保存信任设备

不做二维码（v1.1 再加）。

------

## 3.5 设备发现机制

### 默认：手动刷新按钮

用户点击“刷新” → 手机发 UDP 广播 → 电脑响应。

不做持续广播（避免耗电）。

端口默认：

```
UDP: 45678
WSS: 8765
```

------

## 3.6 WebSocket 协议设计

所有消息为 JSON：

### 心跳

```json
{ "type": "ping" }
{ "type": "pong" }
```

每 25 秒发送一次。

60 秒无响应断开。

------

### 发送文本

```json
{
  "type": "send",
  "mode": "chat" | "realtime",
  "content": "文本内容"
}
```

------

### 重置同步（实时模式）

```json
{
  "type": "reset"
}
```

------

### 暂停状态响应

```json
{
  "type": "paused"
}
```

------

## 3.7 输入注入逻辑

### 默认模式：Unicode 注入

使用：

```
SendInput + KEYEVENTF_UNICODE
```

逐字符注入。

------

### 备用模式：剪贴板粘贴

流程：

1. 保存原剪贴板
2. 写入文本
3. 模拟 Ctrl+V
4. 恢复剪贴板

可在设置中切换。

------

## 3.8 注入模式策略

| 模式     | 行为     |
| -------- | -------- |
| chat     | 全量注入 |
| realtime | 发送增量 |

------

## 3.9 实时模式设计

### 规则：

- Flutter debounce 150ms
- 发送整个文本
- Rust 维护 last_text
- 前缀匹配计算增量
- 仅注入新增字符

------

### 删除情况处理（v1 策略）

不自动处理删除。

提供：

```
↺ 重置同步
```

逻辑：

- Rust 清空 last_text
- Flutter 重新发送完整文本

------

## 3.10 重连策略

指数退避：

```
1s → 2s → 4s → 8s → 16s → 32s → 60s（上限）
```

带 20% 随机抖动。

连续 8 次失败后停止重试并提示用户。

------

## 3.11 系统托盘

菜单：

- 状态
- 暂停接收
- 注入模式切换
- 退出

------

# 📱 四、手机端（Flutter）

------

## 4.1 页面结构

### 首页（默认聊天模式）

```
顶部：设备状态
中间：多行输入框
底部：
[⚡ 实时]   [发送]
```

默认：

- 聊天模式
- 必须点击发送

------

## 4.2 实时模式

开启后：

- debounce 150ms
- 自动发送
- 显示“已同步”提示
- 显示“↺ 重置”按钮

------

## 4.3 连接管理

状态：

- 未连接
- 连接中
- 已连接
- 已暂停
- 断开重连中

------

## 4.4 设备发现

点击刷新：

- 发 UDP 广播
- 收到响应后展示列表

不做持续扫描。

------

## 4.5 安全

- 首次连接必须配对
- 保存 token
- 下次自动连接

------

# 🔐 五、安全策略

v1：

- WSS 加密
- 配对验证码
- 保存信任 token

不做公网。

------

# ⚙ 六、配置项（v1）

手机端：

- 注入方式（Unicode / 剪贴板）
- 默认发送模式（聊天 / 实时）
- 自动重连开关

桌面端：

- 注入方式
- 暂停接收

------

# 🚀 七、开发阶段计划

------

## 阶段 1（核心可用）

- Rust：Unicode 注入
- Flutter：聊天模式
- WSS 基础连接
- 手动输入 IP 连接

目标：成功发送文字。

------

## 阶段 2（局域网体验）

- UDP 刷新发现
- 验证码配对
- token 保存

------

## 阶段 3（稳定性）

- 心跳
- 指数退避
- 自动重连

------

## 阶段 4（实时模式）

- debounce
- 增量计算
- 重置机制

------

## 阶段 5（完善体验）

- 托盘
- 设置页
- fallback 注入
- 打包发布

------

# 🏁 最终 v1 目标标准

满足：

- 连接稳定
- 延迟 < 200ms
- 连续使用 2 小时不崩
- 中文输入稳定
- 不污染剪贴板（默认）

------

# 🎯 未来 v1.1+ 方向

- 二维码配对
- mDNS
- Mac 支持
- 安卓/iOS 后台常驻优化
- 自动 fallback 注入
- 简单统计（字数/今日输入量）

------



# 重要补充

#### 2. Windows 防火墙“沉默拦截”

- **风险点**：Rust 启动 WSS 监听端口（8765）时，Windows 防火墙可能会默默拦截，导致手机扫描不到。
- **解决方案**：
  - **v1 策略**：第一次启动时，检测并尝试添加防火墙白名单规则（需要 `netsh` 命令或相关 Rust crate）。或者手动弹窗提示用户“请允许网络访问”。
  - **AI 指令**：*“请在 Server 启动逻辑中包含对 Windows 防火墙规则的检查，或在 Readme 中明确写出需要手动允许端口 8765/TCP 和 45678/UDP。”*

#### 3. 移动端的“杀后台”与 Socket 断连

- **风险点**：Flutter 锁屏或切后台超过 30 秒，Socket 基本必断。
- **解决方案**：
  - 你文档里提到了“心跳 & 重连”，这很好。
  - **补充策略**：在 Flutter 端监听 `AppLifecycleState`。一旦从后台回到前台（`resumed`），**立刻**触发一次强制重连/状态检查，不要等下一次心跳。
  - **AI 指令**：*“在 Flutter 端实现生命周期监听，当 App 从后台切回前台时，立即检查 WebSocket 状态，如果断开则立刻重连。”*